# Directory Scanner

Simple Python script that checks for existing directories on a target website using a wordlist.

## Requirements
pip install requests

## Run
python directories.py

## Usage
- Enter target URL (without http/https)
- Enter a file containing directory names (one per line)

The script prints discovered directories.``
